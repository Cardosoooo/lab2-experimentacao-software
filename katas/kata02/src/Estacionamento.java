/**
 * Kata 02 - Cobranca de Estacionamento.
 *
 * Calcula o valor a pagar na saida do estacionamento.
 * As regras completas estao em ENUNCIADO.md.
 *
 * Esqueleto: implemente o metodo cobrar.
 */
public class Estacionamento {

    /**
     * Calcula o valor da permanencia.
     *
     * @param entrada    data e hora de entrada, no formato "yyyy-MM-dd HH:mm"
     * @param saida      data e hora de saida, no mesmo formato
     * @param mensalista true quando o veiculo pertence a um assinante mensal
     * @return valor com duas casas decimais, ou "ERRO" quando a entrada e invalida
     */
    public static String cobrar(String entrada, String saida, boolean mensalista) {
        return "";
    }
}
