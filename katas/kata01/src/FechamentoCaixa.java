import java.util.List;

/**
 * Kata 01 - Fechamento de Caixa.
 *
 * Consolida os movimentos de um dia de vendas e devolve a linha de fechamento.
 * As regras completas estao em ENUNCIADO.md.
 *
 * Esqueleto: implemente o metodo fechar.
 */
public class FechamentoCaixa {

    /**
     * Consolida a lista de movimentos do dia.
     *
     * @param movimentos linhas no formato "V;id;valor;forma" ou "E;id"
     * @return linha de fechamento no formato
     *         "bruto=..;liquido=..;taxas=..;vendas=..;estornadas=..;invalidas=.."
     */
    public static String fechar(List<String> movimentos) {
        return "";
    }
}
